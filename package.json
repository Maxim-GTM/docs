{
  "name": "docs",
  "private": true,
  "scripts": {
    "dev": "mintlify dev --no-open",
    "start": "mintlify dev --port ${PORT:-3000} --no-open"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "mintlify": "^4.2.225"
  }
}
